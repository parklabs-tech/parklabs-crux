# Linux Kernel Config

This is not a package, this is just a kernel config that I applied to Linux 6.12.23

If you copy the .config into the linux source directory (`/usr/src/linux-6.12.23` on crux), it should reproduce my kernel.
